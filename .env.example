# This project uses config.json by default.
# Keep this file only as a safe placeholder for tools that expect .env examples.
#
# Main setup:
#   copy config.example.json config.json
#
# Never commit real .env or config.json files.
